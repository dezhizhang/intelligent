# import torch
#
# a = torch.rand(2,2) * 10
# a = a.clamp(1,2)
#
# print(a)

# import torch
#
# a = torch.rand(2,2) * 10
# a = a.clamp(1,2)
# print(a)

# import torch
# a = torch.rand(2,2) * 10
# a = a.clamp(1,2)
# print(a)

import torch
a = torch.rand(2,2) * 10
a = a.clamp(1,2)
print(a)








